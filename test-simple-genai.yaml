apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-compare-simple
  namespace: fresh
  labels:
    app: ai-compare-simple
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ai-compare-simple
  template:
    metadata:
      labels:
        app: ai-compare-simple
    spec:
      containers:
      - name: ai-compare
        image: ravan/genai-observability-demo-app:0.0.6
        ports:
        - containerPort: 8000
        env:
        - name: APP_NAME
          value: "AICompareSimple"
        - name: OLLAMA_ENDPOINT
          value: "http://ollama-service:11434"
        - name: OTLP_ENDPOINT
          value: "http://opentelemetry-collector.suse-observability.svc.cluster.local:4318"
        - name: COLLECT_GPU_STATS
          value: "false"
        resources:
          requests:
            cpu: 400m
            memory: 800Mi
          limits:
            cpu: 600m
            memory: 1000Mi
---
apiVersion: v1
kind: Service
metadata:
  name: ai-compare-simple-service
  namespace: fresh
spec:
  selector:
    app: ai-compare-simple
  ports:
  - port: 8000
    targetPort: 8000
  type: ClusterIP
