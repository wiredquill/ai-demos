# SUSE AI HR Assistant with Sovereign Data Compliance Configuration
# Showcases comprehensive LLM observability with OpenLit integration and regional data sovereignty

# Global configuration
imagePullSecrets:
  - name: application-collection

# Sovereign Data Compliance Configuration
deployment:
  region: "americas"                      # Options: americas, emea, asia-pac
  country: "United States"                # Country within selected region (defaults: USA/Austria/Australia)
  sovereigntyNotice: ""                   # Question for countries with data sovereignty requirements (yes/no)
  acknowledgeDataSovereignty: false       # Confirms compliance with local data sovereignty laws

# Observability endpoints
otlpEndpoint: 'http://opentelemetry-collector.opentelemetry-collector.svc.cluster.local:4318'
ollamaEndpoint: 'http://my-hr-assistant-soveregin-data-ollama:11434'
collectGpuStats: 'false'  # Enable GPU metrics for demo

# Persistent load generator. Replaces the old CronJob approach, which created a
# new pod every cycle and let it terminate — that churn made the load-gen pod
# flap offline/online in the SUSE AI topology view every few minutes.
# The new design runs a single long-lived Deployment instead, so the pod is
# created once and stays up for the lifetime of the release.
#
# The pod polls the app /ask endpoints on this interval. The interval must
# stay comfortably under the topology exporter's TTL (components expire ~2 min
# after their last telemetry) so the SUSE AI view never goes empty; 120s keeps
# everything continuously lit.
loadGenerator:
  # Set false to remove the persistent load-generator pod entirely.
  enabled: true
  # Seconds between polling cycles for the app /ask endpoints.
  intervalSeconds: 45
  # Number of load-generator replicas (keep at 1 for a demo).
  replicas: 1

# Job cleanup settings (still used by the Ollama model-puller Job)
jobs:
  # Time in seconds after which completed jobs are deleted
  ttlSecondsAfterFinished: 300  # 5 minutes

# Official SUSE Application Collection Ollama chart configuration
ollama:
  enabled: true
  image:
    tag: "0.21.2-11.48"
  imagePullSecrets:
    - name: application-collection
  gpu:
    enabled: true
    type: "nvidia"
    number: 1
  hardware:
    type: "nvidia"
  resources:
    requests:
      cpu: "500m"
      memory: "2Gi"
    limits:
      cpu: "4"
      memory: "16Gi"
  persistence:
    enabled: false
    size: 10Gi
  models:
    pull:
    - llama3.2:1b
    - bge-large:latest



# This will set the replicaset count more information can be found here: https://kubernetes.io/docs/concepts/workloads/controllers/replicaset/
replicaCount: 1

# This sets the container image more information can be found here: https://kubernetes.io/docs/concepts/containers/images/
image:
  repository: ghcr.io/wiredquill/llm-experiments/genai-demo
  # This sets the pull policy for images.
  pullPolicy: IfNotPresent
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

# This is to override the chart name.
nameOverride: ""
fullnameOverride: ""



# This section builds out the service account more information can be found here: https://kubernetes.io/docs/concepts/security/service-accounts/
serviceAccount:
  # Specifies whether a service account should be created
  create: true
  # Automatically mount a ServiceAccount's API credentials?
  automount: true
  # Annotations to add to the service account
  annotations: {}
  # The name of the service account to use.
  # If not set and create is true, a name is generated using the fullname template
  name: ""

# This is for setting Kubernetes Annotations to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/annotations/
podAnnotations: {}
# This is for setting Kubernetes Labels to a Pod.
# For more information checkout: https://kubernetes.io/docs/concepts/overview/working-with-objects/labels/
podLabels: {}

podSecurityContext: {}
  # fsGroup: 2000

securityContext: {}
  # capabilities:
  #   drop:
  #   - ALL
  # readOnlyRootFilesystem: true
  # runAsNonRoot: true
  # runAsUser: 1000

# This is for setting up a service more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/
service:
  # This sets the service type more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#publishing-services-service-types
  type: ClusterIP
  # This sets the ports more information can be found here: https://kubernetes.io/docs/concepts/services-networking/service/#field-spec-ports
  port: 8000

resources:
  requests:
    memory: '800Mi'
    cpu: '400m'
  limits:
    memory: '1000Mi'
    cpu: '600m'

nodeSelector: {}

tolerations: []
