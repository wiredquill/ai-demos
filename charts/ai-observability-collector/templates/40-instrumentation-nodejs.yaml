{{- if .Values.instrumentation.nodejs.enabled }}
# Instrumentation CR for Node.js auto-instrumentation.
# Apps opt in per-workload by adding the annotation:
#   instrumentation.opentelemetry.io/inject-nodejs: "true"
# (or "default" to use this as the namespace-wide default).
# The injected SDK exports traces to the collector over OTLP HTTP.
apiVersion: opentelemetry.io/v1alpha1
kind: Instrumentation
metadata:
  name: {{ printf "%s-%s" .Values.collector.name "nodejs" }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "aiobs.labels" . | nindent 4 }}
spec:
  exporter:
    endpoint: {{ include "aiobs.otlpHttpEndpoint" . }}
  propagators:
    - tracecontext
    - baggage
  sampler:
    type: parentbased_traceidratio
    argument: "1"
  nodejs:
    image: ghcr.io/open-telemetry/opentelemetry-operator/autoinstrumentation-nodejs:0.52.1
    resources:
      limits:
        cpu: 500m
        memory: 128Mi
      requests:
        cpu: 50m
        memory: 32Mi
{{- end }}
