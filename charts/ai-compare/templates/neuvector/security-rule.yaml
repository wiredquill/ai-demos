{{- if .Values.neuvector.enabled }}
apiVersion: neuvector.com/v1
kind: NvSecurityRule
metadata:
  name: {{ include "ai-compare.fullname" . }}-security-rule
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ai-compare.labels" . | nindent 4 }}
spec:
  name: {{ .Release.Namespace }}.{{ include "ai-compare.fullname" . }}
  comment: "Security group for AI Compare application with DLP monitoring"
  mode: discover
  kind: container
  dlpSensors:
    - name: {{ include "ai-compare.fullname" . }}-dlp-rule
  ingress:
    - action: allow
      name: Allow from Open WebUI
      selector: open-webui
    - action: allow
      name: Allow from Ollama
      selector: ollama
  egress:
    - action: allow
      name: Allow external traffic
      selector: external
      ports: any
{{- end }}
